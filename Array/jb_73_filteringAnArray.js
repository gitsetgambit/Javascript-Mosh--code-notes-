const numbers = [1,-1,2,3,0]

// here we used arrow function where value is compared with 0 and printed the 
// filtered values in log screen
const filtered = numbers.filter(value => value >= 0)
console.log(filtered);
