// of is similar to in but in this we don't need
// to deal with indexes in the array and it 
// logs all the elements automatically 

const colors = ['dragon','pheonix','unicorn'];
for(let index of colors)
    console.log(index);


// for-in -> we use it to iterate over the properties of object
// for-of -> we use it to iterate over elements and items of array