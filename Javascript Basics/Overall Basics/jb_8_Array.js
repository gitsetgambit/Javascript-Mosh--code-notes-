// thats how we can declare a array
let selectedColors = ["red", "blue"];

// thats how we can assign value at a particular index
selectedColors[2] = "black";

// thats how we can push any element in the array at the
selectedColors.push("green")

// .length property is used for the lenth of the array
console.log(selectedColors.length);
