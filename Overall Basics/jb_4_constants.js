// if we don't want to reasign then "const" is used
// for to keep the value as it is
const InterestRate = 0.5;

// it'll give error if we try to change a const variable
InterestRate = 1;

// just simple printing
console.log(InterestRate);
