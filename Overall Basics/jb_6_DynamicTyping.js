let name = "aditya"; // string literal
let age = 23; // Number Literal
let isApproved = false; // boolean Lteral
let FirstName = undefined; // we mostly use null instead of undefined
let selectedColor = null;

// for finding the type of the variable declared
// above
console.log(typeof age);
console.log(typeof isApproved);
console.log(typeof FirstName);
console.log(typeof selectedColor);
